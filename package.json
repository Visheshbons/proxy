{
  "name": "proxy",
  "version": "1.0.0",
  "description": "A comprehensive learning web app for students from Year 1 to 13 (Trust)",
  "private": true,
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "echo 'No build step required for Node.js application' && exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "education",
    "learning",
    "nodejs",
    "express",
    "ejs"
  ],
  "dependencies": {
    "chalk": "^5.6.2",
    "ejs": "^3.1.10",
    "express": "^5.1.0"
  }
}
